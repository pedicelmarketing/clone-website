# Animation Audit

> Installed blank template — read-only. Copy it to `<project>/reports/animation-audit.md` and fill the project-local copy. Never write project data into the installed skill.
>
> One row per meaningful in-scope animation. Mark estimated timings with `(est.)` and unknown values as `Unknown`. Include WebGL/canvas, video-driven, and audio-synced motion when those modules are active.

| # | Element / section | Trigger | Duration | Easing | Delay | Start state | End state | Transforms / opacity | Parallax / pin / scrub | Sequencing | Asset or module |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| | | | | | | | | | | | |

## Motion evidence summary

- Observed motion system(s) and evidence:
- Shared measured values (durations, easings, thresholds):
- Declared states exercised:
- Blocked, unexercised, or uncertain behavior:
