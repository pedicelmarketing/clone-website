# Asset Preservation Table

> Installed blank template — read-only. Copy it to `<project>/reports/asset-preservation-table.md` and fill the project-local copy. Never write project data into the installed skill.
>
> Required before constructing any in-scope baseline section affected by a major motion, media, audio, WebGL, video, runtime, or route-specific asset system.
> Status must be exactly one of: `Original` | `Local Copy` | `Embed` | `Kept External` | `User-Supplied Baseline Asset` | `Recreated` | `Recreated From Observation` | `Approximated` | `Blocked` | `Unknown`.
> `User-Supplied Baseline Asset` means supplied before or during faithful baseline construction because the observed asset cannot be acquired or used lawfully. It is not a post-delivery asset swap.
> Reminder: paid fonts, provider-streamed media, external media, and protected assets are never automatically downloadable. `Local Copy` requires authorization + license + technical permission.

| Asset | Source / evidence (URL or authorized code path) | Status | Baseline handling and constraints | Fidelity impact |
| --- | --- | --- | --- | --- |
| | | | | |

## Baseline approximations requiring approval

> Any row with `Approximated` requires user approval before it is used in the requested faithful copy. Rows describe baseline construction only.

-
