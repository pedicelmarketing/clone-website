# motion-recipes/ — placeholder

This directory will hold the AOS / Motion / GSAP snippets in M3+. The empty
directory is preserved here so the file tree matches `research/integration-plan.md`
§2 from M1 onward.

When M3 lands, this README will be replaced with the actual snippet files:
- `aos-reveal.json` — AOS data-aos values per section role
- `motion-orchestrated.tsx` — Motion page-load orchestrator
- `gsap-scrolltrigger.ts` — only when justified
